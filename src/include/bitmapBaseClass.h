#include <stdint.h>
class bitmapBaseClass
/**
 * 这个基类只处理项为一个bit宽度的bitmap
 */
{
private:
    /* data */
public:
    bitmapBaseClass(/* args */);
    ~bitmapBaseClass();
};

bitmapBaseClass::bitmapBaseClass(/* args */)
{
}

bitmapBaseClass::~bitmapBaseClass()
{
}
class bitmap_wide_entry_base extends bitmapBaseClass
{
private:
    /* data */
public:
    bitmap_wide_entry_base(/* args */);
    ~bitmap_wide_entry_base();
};

bitmap_wide_entry_base::bitmap_wide_entry_base(/* args */)
{
}

bitmap_wide_entry_base::~bitmap_wide_entry_base()
{
}

