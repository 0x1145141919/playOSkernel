本文档是内核调试框架的计划书:
众所周知x86里面的调试可以用中断int 3当断点，那么可以在创建对应的中断处理历程函数
显然调试陷阱所使用的是特殊的栈
下面是我对大体逻辑的设计:
1.收集各种寄存器帧栈信息，
2.伪造栈iret退出中断，但是跳转到专门的调试业务函数
3.在调试函数中轮询读写串口，
3.1轮询通过串口写内容到客户机
3.2轮询读串口，直到独到"EOF"或者其它终结标志
3.3解析语法，看情况是3.1，还是重启/关机/继续执行